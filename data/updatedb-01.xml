<!DOCTYPE etl SYSTEM "http://scriptella.javaforge.com/dtd/etl.dtd">
<etl>
  <description></description>
  <!--    <properties>
    <include href="script.properties"/>
    driver=org.jdcDriver
    </properties> -->
  <connection id="con1" driver="mysql"
    url="jdbc:mysql://localhost/kune_test" user="kune"
    password="db4kune" />

  <connection id="countries-in" driver="csv" url="country_data.csv"
    classpath="opencsv.jar">
    #Use empty quote to turn off quoting quote=" separator=,
  </connection>

  <query connection-id="countries-in">
    <script connection-id="con1">
      INSERT INTO globalize_countries VALUES (?id, ?code, ?english_name,
      ?date_format, ?currency_format, ?currency_code, ?thousands_sep,
      ?decimal_sep, ?currency_decimal_sep, ?number_grouping_scheme);
    </script>
  </query>

</etl>
